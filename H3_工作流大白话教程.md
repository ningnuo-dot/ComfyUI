# MiniMax H3 工作流大白话教程（本机 ComfyUI2 / 8189 实况版）

> 2026-09-11 编写。对应环境：`D:\GitHub\ComfyUI2`（ComfyUI 0.35.0，端口 8189）。
> 模板位置：浏览器打开 http://127.0.0.1:8189 → 左侧模板库（Templates）→ 视频（Video）分类 → MiniMax H3 系列。
> 本机模型文件与官方模板默认名不同（量化版），对照表见文末。

## 0. 一条 H3 视频是怎么诞生的

把整条工作流想成一条**剧组流水线**，只有五道工序：

```text
① 选角组：把"大脑"装进机器        → UNETLoader / LoRA / 编码器 / VAE
② 编剧组：让机器听懂你要什么      → CLIPTextEncode（文字 → 数字向量）
③ 摄制组：定规格 + 喂参考         → ImageToVideo / ReferenceToVideo / AddGuide
④ 拍摄组：一步步把噪声"雕"成视频   → 噪声 + 采样器 + 步数 + 指导
⑤ 后期组：冲洗成片                → VAEDecode → CreateVideo → SaveVideo
```

H3 特殊的地方只有两个：**它同时生成画面和声音**（所以 VAE 有两个、解码也有两个）；**它认一种叫 `<Picture 1>` / `<Video 1>` 的引用标签**（参考素材全靠这个机制进提示词）。

---

## 1. 五个官方模板怎么选

| 模板 | 一句话 | 你给什么 |
|---|---|---|
| t2v 文生视频 | 纯文字想象 | 只写提示词 |
| i2v 图生视频 | 给一张定妆照让它动起来 | 1 张图（可加尾帧=首尾帧模式） |
| **r2v 参考生视频** | **咱们的女装工作流主战场** | 最多 9 图 + 3 视频 + 3 音频 |
| multiframe 多帧参考 | 在视频中间某秒"钉"一张参考帧 | 图 + 指定锚点时间 |
| i2v_continuation 续接 | 拿上一段的最后一帧当下一段开头 | 上一段的尾帧图 |

模板打开后你会看到一个大盒子叫「子图（Subgraph）」——官方把 ①③④⑤ 大部分节点打包进去了，双击盒子可以进去看内部。**五个模板的骨架完全一样，差别只在③摄制组用哪个节点。**

---

## 2. 逐节点大白话

### ① 选角组：装"大脑"

**UNETLoader（加载主模型）** —— 装的是 H3 的"想象力本体"（官方叫 diffusion model / transformer）。
- 下拉里选哪个文件，决定了你在跑 FL2VA 还是 Ref2VA：`...fl2va...` = 只懂图/文，`...ref2va...` = 懂参考素材。
- `weight_dtype` 保持 `default`（咱们用的量化模型自带精度信息，别乱改成 fp16 会加倍显存）。

**LoraLoaderModelOnly（加速外挂）** —— 给大脑接一个"快进补丁"。
- 原版 H3 要跑 20 步；挂上 turbo LoRA 后 4~8 步就出片，质量损失很小。
- `strength_model` 保持 1.0；**挂了 LoRA 就必须把步数降到 4~8**，还跑 20 步等于白挂。
- 不想用加速就直接删掉这个节点，把连线改回直连 UNETLoader（模板里的开关节点干的就是这事）。

**CLIPLoader / CLIPLoaderGGUF（翻译官）** —— H3 的"阅读理解"用的是 Qwen3-VL-32B 大语言模型，这个节点把它装进来。
- `type` 必须选 **`minimax`**——这告诉 ComfyUI"这是 H3 定制版编码器"，选错直接崩。
- 咱们机器用的是 GGUF 量化版（`CLIPLoaderGGUF` 节点），文件是 `Qwen3-VL-32B-...Q2_K_XL.gguf`。
- **本机专属**：`device` 选 `cpu`。这是我们打的补丁选项——12GB 显存装不下 11GB 编码器+11GB 主模型，低显存时 GGUF 版会崩（access violation），固定在 CPU 上稳，代价是读提示词慢几分钟。

**VAELoader ×2（冲洗机，装两台）** —— VAE 负责把模型内部的"压缩表示"还原成真画面/真声音。
- 视频用一台：`minimax_h3_video_vae_int8_convrot.safetensors`（咱们用的减半版，省 2GB）。
- 音频用一台：`minimax_h3_audio_vae_fp32.safetensors`。
- 两台都要接给后面的解码节点，**缺音频 VAE = 成片没声音**。

### ② 编剧组：让机器听懂

**CLIPTextEncode（写需求单）** —— 你写的提示词在这里被 32B 编码器读一遍，变成模型能运算的数字。
- H3 的提示词风格：**先说风格/镜头，再说主体干什么，按时间轴写动作**。
- 引用参考素材时用标签：`<Picture 1>`（第几张参考图）、`<Video 1>`（第几段参考视频）、`<Audio 1>`——编号从 1 数，和③里接线的顺序对应。

### ③ 摄制组：定规格 + 喂参考（H3 的特色都在这）

**ResolutionSelector（选画幅）** —— 官方推荐的取景器：选 16:9 / 9:16 / 1:1 + 百万像素档位，自动算出宽高。
- 记住一个硬规则：**总像素别超 1344×768 ≈ 103 万**，1.0 档(1376×768)会超模型上限出坏片；0.4 档(864×480 / 竖屏 480×864)是咱们的常用档。

**宽/高/length（定时长）** —— H3 的帧数有个硬网格：**只能是 17 的倍数 +5**（124 帧=5.2 秒、240 帧=10 秒、340 帧≈14.2 秒、362 帧=15 秒封顶）。
- 模型只在 **124~362 帧**之间训练过，短于 5 秒长于 15 秒都是自担风险。
- 模板里那个长得吓人的 `ComfyMathExpression`（`max(5, round(a*24)) + ...`）干的就是"你输秒数，我自动凑到最近的合法帧数"。

**MiniMaxH3ImageToVideo（FL2VA：定妆照模式）** —— 文生 / 首帧 / 尾帧 / 首尾帧都在这一个节点。
- 接 `image` = 首帧（画面从这张图开始动）；接 `end_image` = 尾帧（画面必须停在这张图）；两个都接 = 首尾帧约束。
- 它同时输出两根线：`conditioning`（需求单）和 `latent`（底片），分别往④接。

**MiniMaxH3ReferenceToVideo（Ref2VA：咱们的参考模式）** —— 核心能力节点。
- 参考接口是"自动生长"的：点节点上的 `+` 号加一排口子，`ref_image_1/2/3...` 最多 9 张图，`ref_video_1...` 最多 3 段视频（每段 2-15 秒、24fps），`ref_audio_...` 最多 3 段音频。
- 图片顺序 = `<Picture>` 编号；视频顺序 = `<Video>` 编号，提示词里靠编号引用。
- `ref_image_size`：`match`（参考图缩到生成分辨率，快）vs `max`（参考图吃到 2048 短边，**身份保真最好但慢好几倍**）——要脸更像选 max，赶时间选 match。
- ⚠️ **喂参考视频要用 LoadVideo + GetVideoComponents 两个节点转一道**（拿 IMAGE 批量帧那根线接给 ref_video）。别用"从文件夹加载图片"节点——那个吐的是"图片清单"，H3 会把每张图当单独任务，直接报错。
- ⚠️ 本机教训：340 帧 + 341 帧参考会**撑爆 16GB 内存把进程无声杀死**——参考和输出都控制在 10 秒（240 帧）内更稳。

**MiniMaxH3AddGuide（多帧锚点）** —— 在成片的**第 N 帧**强行钉一张图（或一段音频）。
- 用法：接 `positive`（条件线串过来）+ `latent` + `frame_idx`（钉在第几帧）+ 图 + 视频 VAE。
- 典型场景：转身到中段必须呈现某个Pose——把那张Pose图钉在对应帧上。

**MiniMaxH3SigmaShift（高级旋钮）** —— 调降噪节奏的偏移，官方模板都没用它，新手忽略。

### ④ 拍摄组：把噪声雕成视频

理解成一个比喻：**从一团雪花噪点里，按需求单一步步"显影"出画面**。

- **RandomNoise（底片颗粒）** —— `noise_seed` 是随机种子：同种子+同参数=可复现；调种子=换个底稿重抽。对照实验必须锁死 `fixed`。
- **KSamplerSelect（雕刻刀型号）** —— 选采样器，H3 用官方指定的 `res_multistep`，不用动。
- **BasicScheduler（时刻表）** —— `steps` 走几步 + `scheduler` 节奏（用 `simple`）。**挂 turbo LoRA 时 4~8 步，不挂 20 步**。
- **BasicGuider（监工）** —— 拿着需求单（conditioning）监督大脑怎么雕。
- **SamplerCustomAdvanced（总装台）** —— 五根线全汇到这：噪声+监工+刀+时刻表+底片(latent)，输出雕好的"潜视频"。

### ⑤ 后期组：冲洗

- **VAEDecode** —— 潜视频 → 真画面（用视频 VAE）。
- **VAEDecodeAudio** —— 潜空间里的音轨 → 真声音（用音频 VAE）。**不接它成片就是默片**。
- **CreateVideo** —— 画面帧 + 声音合成，`fps=24` 写死（H3 原生就是 24）。
- **SaveVideo** —— 落盘到 `output/video/MiniMax_H3/`，格式 auto 即可。

---

## 3. 咱们机器 vs 官方模板的对照表

打开官方模板会提示下载官方大文件（都是 BF16 巨无霸，咱 12GB 显存放不下），**全部点掉，手动换成本机文件**：

| 节点 | 官方模板默认 | 本机替换为 |
|---|---|---|
| UNETLoader | minimax_h3_fl2va_pruned_int8_convrot.safetensors | **minimax_h3_fl2va_pruned_w4a8_mixed.safetensors** |
| UNETLoader (r2v) | minimax_h3_ref2va_pruned_int8_convrot.safetensors | **minimax_h3_ref2va_pruned_w4a8_mixed.safetensors** |
| CLIPLoader | qwen3vl_32b_minimax_h3_nvfp4_awq.safetensors | 换成 **CLIPLoaderGGUF** 节点：Q2_K_XL.gguf + type=minimax + **device=cpu** |
| VAELoader (视频) | minimax_h3_video_vae_fp16.safetensors | **minimax_h3_video_vae_int8_convrot.safetensors** |
| VAELoader (音频) | minimax_h3_audio_vae_fp32.safetensors | 同名，不用换 |
| LoraLoader | minimax_h3_fl2v_turbo_8step / ref2v_turbo_4step 官方版 | **MiniMax-H3-FL2VA-Acc-8Step_pruned_comfy / Ref2VA 版**（步数用 8） |

## 4. 参数速查

| 想要 | 设置 |
|---|---|
| 时长 5 秒 / 10 秒 / 15 秒 | length = 124 / 240 / 362（必须 17k+5） |
| 竖屏抖音款 | 480×864（0.4MP 档） |
| 横屏最高画质 | 1344×768（别用 1.0MP 档！） |
| 可复现实验 | RandomNoise 种子 fixed |
| 脸更像四视图 | ref_image_size=max（慢） |
| 出片没声音 | 检查音频 VAE + VAEDecodeAudio 是否都接了 |
| 换任务前显存不够 | 设置里点 Free memory，或调 `/free` 接口 |
